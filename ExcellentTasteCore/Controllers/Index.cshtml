@page
@model ExcellentTasteCore.Controllers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConsumptieGroep[0].ConsumptieGroepNaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConsumptieGroep[0].Consumptie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ConsumptieGroep) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConsumptieGroepNaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Consumptie.ConsumptieCode)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ConsumptieGroepCode">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ConsumptieGroepCode">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ConsumptieGroepCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
